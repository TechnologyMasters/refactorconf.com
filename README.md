## [Website](https://refactorconf.com/)

### Local Development

Clone repo

```bash
git clone --recursive 
```

If you already have cloned the repository:

```bash
git submodule update --init --recursive
```

### Run with `Docker Compose`

```bash
docker-compose up
```
